package com.pps.repository;

import com.pps.entity.UserOwnCourse;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UserOwnCourseRepository extends JpaRepository<UserOwnCourse, Long> {
}
